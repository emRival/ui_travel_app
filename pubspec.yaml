name: ui_slacing_travel_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  convex_bottom_bar: ^3.2.0
  device_preview: ^1.2.0
  flutter:
    sdk: flutter
  flutter_svg: ^0.22.0
  google_fonts: ^6.2.1
  readmore: ^3.0.0
  svg_icon: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/

  fonts:
    - family: Hiatus
      fonts:
        - asset: assets/fonts/Hiatus.ttf
